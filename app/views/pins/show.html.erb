<p id="notice"><%= notice %></p>

<p>
  <strong>Description:</strong>
  <%= @pin.description %>
</p>
<!-- Here the pin needs @ before it because here we're using the pin defined in the controller which is @pin, and only includingthis around the edit link so that anyone can go back-->
<%if @pin.user == current_user %>
<%= link_to 'Edit', edit_pin_path(@pin) %> |
<% end %>
<%= link_to 'Back', pins_path %>
